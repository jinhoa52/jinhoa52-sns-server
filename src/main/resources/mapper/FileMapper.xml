<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.koobin.snsserver.mapper.FileMapper">

  <insert id="insertFile" parameterType="me.koobin.snsserver.model.FileInfo"
    useGeneratedKeys="true" keyColumn="id" keyProperty="id">
    insert into file (file_name, save_file_name, content_type, file_storage, path)
    VALUES (#{fileName}, #{saveFileName}, #{contentType}, #{fileStorage}, #{path})
  </insert>

  <insert id="insertFiles" parameterType="list"
    useGeneratedKeys="true" keyColumn="id" keyProperty="id">
    insert into file (file_name, save_file_name, content_type, file_storage, path)
    VALUES
    <foreach collection="fileInfos" item="info" separator=",">
      (#{info.fileName}, #{info.saveFileName}, #{info.contentType}, #{info.fileStorage},
      #{info.path})
    </foreach>
  </insert>

  <delete id="deleteFileBy" parameterType="long">
    UPDATE FILE
    SET DELETE_FLAG = 1,
        modify_date = now()
    WHERE ID = #{id}
  </delete>
  <delete id="deleteFileByIn">
    UPDATE FILE
    SET DELETE_FLAG = 1,
    modify_date = now()
    WHERE ID IN
    <foreach collection="fileInfos" item="fileInfo" open="(" close=")" separator=",">
      #{fileInfo.id}
    </foreach>
  </delete>

  <select id="findById" resultType="me.koobin.snsserver.model.FileInfo" parameterType="long">
    select *
    from file
    where id = #{id}
  </select>
  <select id="findByIdIn" resultType="me.koobin.snsserver.model.FileInfo">
    select *
    from file
    where id in
    <foreach collection="fileIds" item="id" open="(" close=")" separator=",">
      #{id}
    </foreach>
  </select>

</mapper>